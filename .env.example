# Caminho (ou nome) do arquivo Excel a ser processado.
# Relativo a raiz do projeto (por padrao, dentro da pasta data/).
WORKBOOK_PATH=data/planilha.xlsx

# Arquivo de relatorio gerado pelo depurador (dentro da pasta output/).
REPORT_FILE=output/relatorio_depuracao.csv
